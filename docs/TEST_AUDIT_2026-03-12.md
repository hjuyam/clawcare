# Test Audit — 2026-03-12

## Scope
- Branch: `main` (pulled latest)
- Commands executed:
  - `npm test`
  - `npm run test:e2e`
  - `npm run build`
  - `npm run lint` (not run — build already runs lint/typecheck step; see results below)

---

## 1) Test Results Summary

| Command | Status | Duration | Notes |
| --- | --- | --- | --- |
| `npm test` | ✅ Pass | 9.68s (vitest duration 7.67s) | 9 test files, 20 tests passed |
| `npm run test:e2e` | ❌ Fail | 139.38s | 1 failing test: config placeholder text not found |
| `npm run build` | ❌ Fail | 51.51s | Type error: missing typings for `diff` in `app/api/config/preview_diff/route.ts` |

### Failure details

**E2E failure**
- Test: `tests/e2e/core-pages.spec.ts:36:3` → `core pages (mock gateway) › config placeholder`
- Error:
  - `getByText('占位：Basic Settings + Config Center（只读→启用编辑→diff→apply→rollback）。')`
  - Expected: visible; Actual: element not found
- Attachments generated by Playwright:
  - Screenshot: `test-results/core-pages-core-pages-mock-gateway-config-placeholder-chromium/test-failed-1.png`
  - Video: `test-results/core-pages-core-pages-mock-gateway-config-placeholder-chromium/video.webm`
  - Trace: `test-results/core-pages-core-pages-mock-gateway-config-placeholder-chromium/trace.zip`

**Build failure**
- File: `app/api/config/preview_diff/route.ts`
- Error: `Could not find a declaration file for module 'diff'... Try npm i --save-dev @types/diff`
- Failing line: `import { diffJson } from "diff";`

---

## 2) Coverage Summary (What’s Covered)

### Unit/API (Vitest)
- **Ops**
  - restart gateway (`app/api/ops/__tests__/restart_gateway.test.ts`)
  - cleanup (`app/api/ops/__tests__/cleanup.test.ts`)
  - self_check (`app/api/ops/__tests__/self_check.test.ts`)
  - diagnostics bundle + runs (`app/api/ops/__tests__/diagnostics_bundle.test.ts`, `diagnostics_bundle_runs.test.ts`)
- **Config**
  - safe mode enforcement (`app/api/config/__tests__/safe_mode.test.ts`)
  - apply/rollback runs (`app/api/config/__tests__/apply_rollback_runs.test.ts`)
- **Runs**
  - runs API basics (`app/api/runs/__tests__/runs.test.ts`)
- **Security**
  - RBAC denial + safe mode block (`tests/security/policy.test.ts`)

### E2E (Playwright)
- **Core UI (mock gateway)**
  - Home page capabilities render
  - Ops page self_check API call (viewer role)
  - Security page placeholder text
  - **Config page placeholder text (currently failing)**
- **M1 Auth/RBAC/Safe mode contract** (`tests/e2e/security-m1.spec.ts`)
  - Unauthenticated access denied
  - Viewer denied high-risk ops
  - Admin allowed high-risk ops
  - Safe mode blocks admin high-risk ops

---

## 3) Test Gaps (Prioritized)

### P0 (Blocking / must add)
- **Runs/tasks critical flows**
  - Create run → run lifecycle transitions → run status/runs list
  - Tasks within a run: creation, assignment, completion, failure handling
  - API + UI integration tests for runs/tasks (currently only basic unit coverage on runs API)

### P1 (High)
- **Config Center functional coverage**
  - Read current config → enable edit → diff preview → apply → rollback
  - Validation errors & permission checks
  - UI coverage beyond placeholder text
- **Ops create run**
  - End-to-end flow (UI + API) for “ops create run” (if feature exists)

### P2 (Medium)
- **Regression & edge cases**
  - API error shapes for non-happy paths
  - Permissions matrix (viewer/operator/admin) for config + ops beyond current tests
  - Visual/regression coverage for core pages

---

## 4) Risks & Blockers

| Area | Risk | Level | Impact |
| --- | --- | --- | --- |
| E2E | Config page placeholder text mismatch | **P1** | Failing CI / missing UI contract; likely content change or i18n drift |
| Build | Missing `@types/diff` typing for new `diff` import | **P0 (Blocker)** | Build fails; blocks release & CI |
| Coverage | No end-to-end runs/tasks coverage | **P0 (Quality risk)** | Critical flows unverified; regression risk |
| Coverage | Config Center functional tests missing | **P1** | High-risk config changes untested |

---

## Suggested Next Actions
1. **Fix build** by adding typings: `npm i -D @types/diff` or add local `types/diff.d.ts`.
2. **Update E2E config placeholder** text or page content to re-align.
3. Add **runs/tasks** and **config center** end-to-end coverage (see P0/P1 gaps).

---

## Commit Suggestion (not executed)
```
fix(test): repair build typing + stabilize config placeholder e2e

- add @types/diff for preview_diff route
- align config placeholder copy or adjust test selector
- consider adding runs/tasks & config center e2e coverage
```
